node_modules/
dist/
.env
*.log
.DS_Store
coverage/
*.tsbuildinfo
 
# Known: nested eslint@8.x peer-dep vuln from typescript-eslint meta-package (dev-only, non-runtime).
# Tracked — do not `npm audit fix --force` blindly; verify flat-config compatibility first.
